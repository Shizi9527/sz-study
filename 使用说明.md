# 📚 dist 网页刷题版 · 使用说明

**12 套题库（共 1317 题）**，双击即可刷题，无需联网、无需安装。

## 🚀 使用方法
1. 打开 `dist` 文件夹
2. **双击 `index.html`**（Chrome / Edge 均可）
3. 开始刷题 🎉

> ⚠️ 请保持 `index.html`、`banks.js`、`data/` 文件夹在同一目录（不要单独移动文件）。

## ✨ 功能一览
| 功能 | 说明 |
|------|------|
| 📁 **12 套题库切换** | 顶部下拉切换（金融市场基础 LC300、证券法律法规 300题、各 3 套终极押题）|
| ✍️ **答题 / 📖 背题模式** | 提交自动判对错；背题模式点击「揭晓答案」 |
| 📝 **题型筛选** | 单选 / 多选 / 判断 分模块练习 |
| ⚠️ **错题本** | 「错题」按钮集中复习做错的题 |
| 🔀 **随机顺序** | 打乱题目防机械记忆 |
| 📊 **统计面板 / 进度条** | 实时显示 已作答 / 答对 / 答错 |
| 💾 **本地保存** | 每套题库进度与错题独立存浏览器，关闭重开仍在 |

## 🔄 如何重新生成（PDF 更新后）
```bash
python parse_pdf2.py   # 重新解析 pdf_input 的 12 个 PDF -> web_data/
python build_web.py    # 生成 dist/index.html + banks.js + data/
```
> 依赖：`pip install pdfplumber`

## ❓ 常见问题
- **打开是空白？** 换 Chrome / Edge；确认 `data/` 文件夹与 `index.html` 同级。
- **进度会丢吗？** 存在浏览器 `localStorage`，换浏览器/清缓存会丢失。
- **手机能用吗？** 能，三种方式任选：
  1. **离线单文件版**（推荐）：用上一级目录 `dist_single/index.html`，把它发到手机，用浏览器打开即刷，无需电脑；
  2. **局域网**：双击 `手机局域网刷题.bat`，手机和电脑连同一 WiFi，手机浏览器打开窗口里显示的 `http://IP:8765`；
  3. **公网**：把 `dist` 文件夹托管到 Gitee Pages / GitHub Pages（免费静态托管），手机随时随地访问网址。